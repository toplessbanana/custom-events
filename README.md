#  custom-events

### Usage
```
OVERVIEW: Calls jamf custom events to run concurrently.

USAGE: custom-events [<events> ...]

ARGUMENTS:
  <events>                The events to call.

OPTIONS:
  --version               Show the version.
  -h, --help              Show help information.
```
